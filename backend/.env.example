# Backend reads environment from the repository root.
# Copy the shared template:
#
#   cp ../.env.example ../.env
#
# For local runs inside backend/ only, you may symlink:
#   ln -sf ../.env .env
