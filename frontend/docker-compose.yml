version: "3.9"

services:
  react-dev:
    build:
      context: .
    container_name: react
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true